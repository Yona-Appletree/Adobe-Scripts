// I guess randomly place a number of layers along comp// while also adding opacity & levels tansitions//// created by JK Keller, dilettante coder// http://jk-keller.com//myComp = app.project.activeItem;/* alert(myComp.layer(2).time); *//* 1/24 *//* alert(myComp.layer(2).audioLevels.valueAtTime(2, true)); */var myTimes = [];for (k=0; k<70; k++) {  myTimes.push(Math.ceil((Math.random()*(22*60+50))*24)/24);};function sortNumber(a,b) {  return a - b;}myTimes.sort(sortNumber);for (i=0; i<70; i++) {  var myLayer = Math.ceil(Math.random()*23);  var myTime = myTimes[i];  for (j=1 ; j<24; j++) {  	var pOpacity = myComp.layer(j).opacity;  	var pLevels = myComp.layer(j).audioLevels;    if (myLayer == j) {      if (pOpacity.valueAtTime(myTime, true) != 100) {      	pOpacity.setValueAtTime(myTime-(1/24.0), pOpacity.valueAtTime(myTime, true));      	pLevels.setValueAtTime(myTime-(1/24.0), pLevels.valueAtTime(myTime, true));      	pOpacity.setValueAtTime(myTime, 100);      	pLevels.setValueAtTime(myTime, [0,0]);      }    } else {      if (pOpacity.valueAtTime(myTime, true) != 0) {      	pOpacity.setValueAtTime(myTime-(1/24.0), pOpacity.valueAtTime(myTime, true));      	pLevels.setValueAtTime(myTime-(1/24.0), pLevels.valueAtTime(myTime, true));      	pOpacity.setValueAtTime(myTime, 0);      	pLevels.setValueAtTime(myTime, [-40,-40]);    	}    }  }};